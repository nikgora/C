#ifndef C_AIN_TASK_1_ADRES_H
#define C_AIN_TASK_1_ADRES_H

#endif //C_AIN_TASK_1_ADRES_H

#include <string.h>
#include <math.h>
#include <stdio.h>
#include <stdlib.h>

#define N 100


void print_adres(char *str[]);//вывод строки в прямом порядке принимает строку

void print_reverse_adres(char *str[]);//вывод строки в обратном порядке принимает строку

char *reverse_adres(char *str[]);//инвертирование строки принимает строку результат строка

char *scanword_adres(char *word[]);//ввод строки  принимает строку в которую надо записать строку результат строка

char *skipspace_adres(
        char *word[]);//пропуск серии пробелов принимает строку которую надо отфарматировать результат отфаромотированая строка

